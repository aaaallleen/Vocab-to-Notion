{
  "name": "nextjs-chrome-extension",
  "private": true,
  "scripts": {
    "dev": "npm-watch",
    "build": "next build && next export && npm run lint:build",
    "lint:build": "rimraf out/assets && move out/_next out/assets",
    "lint": "next lint"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.9",
    "next": "12.1.6",
    "react": "18.1.0",
    "react-dom": "18.1.0"
  },
  "devDependencies": {
    "eslint": "8.16.0",
    "eslint-config-next": "12.1.6",
    "npm-watch": "^0.11.0",
    "rimraf": "^5.0.1"
  },
  "watch": {
    "build": {
      "patterns": [
        ".",
        "styles/**",
        "pages/**",
        "public/**"
      ],
      "ignore": [
        "out",
        "node_modules",
        ".next"
      ],
      "extensions": [
        "js",
        "json",
        "lock",
        "tsx",
        "ts"
      ]
    }
  }
}
